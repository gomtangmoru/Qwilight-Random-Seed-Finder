[project]
name = "qwilight-random-seed-finder"
version = "0.1.0"
description = "Qwilight Seed Finder"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.1.1",
    "pyinstaller>=6.19.0",
    "pywebview>=6.1",
]
